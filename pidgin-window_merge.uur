#!/bin/bash

name="window_merge"
url="https://github.com/dm0-/${name}"
depends=(git libtool autoconf pidgin pidgin-dev)

build () {
	mkdir -p m4
	autoreconf -fi
	./configure
	sudo make distcheck
}

package () {
	sudo make install
}
