#!/bin/bash

name="window_merge"
url="https://github.com/dm0-/${name}"
depends=(git libtool autoconf pidgin pidgin-dev)

build () {
	cd $1
	mkdir m4
	autoreconf -fi
	./configure
	sudo make distcheck
}

package () {
	cd $1
	sudo make install
}
