#!/bin/bash

url="https://github.com/dylex/slack-libpurple"
depends=(libpurple-dev build-essential libcurl4-openssl-dev)

build () {
	patch -p1 api-token-in-passwd.patch
	make
}

package () {
	sudo make install
}
