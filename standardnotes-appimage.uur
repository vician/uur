#!/bin/bash

type="AppImage"
ext="${type}"
version="1.2.7"
file="standard-notes-${version}-x86_64.${ext}"
url="https://github.com/standardnotes/desktop/releases/download/v${version}/$file"
depends=()

build () {
	;;
}

package () {
	;;
}

uninstall () {
	rm ~/.local/share/applications/appimagekit-standard-notes.desktop
}
